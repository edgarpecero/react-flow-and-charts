import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        background: {
            default: 'orange',
        },
        primary: {
            main: '#000',
        },
        secondary: {
            main: '#dc004e',
        },
    },
});

export default theme;
